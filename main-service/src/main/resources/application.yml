server:
  port: 8080

logging:
  level:
    httpclient:
      wire: DEBUG
    org:
      springframework:
        web:
          client:
            RestTemplate: DEBUG
        apache:
          http: DEBUG

ewm-server:
  url: ${EVM_SERVER_URL:http://localhost:8080}

